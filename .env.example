# Supabase project used for user testing.
# Copy this file to .env and fill in the values from your test Supabase project
# (Project Settings > API). Keep .env out of git — it's already gitignored.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
