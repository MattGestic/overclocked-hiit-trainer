# OverClocked HITT Supabase project (xilnfrswpirjhzitxahh).
# Copy this file to .env and fill in the values from Project Settings > API
# (use the "publishable key", formerly called the anon key).
# Keep .env out of git — it's already gitignored.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
